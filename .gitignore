# === ДАННЫЕ (содержат зашифрованные пароли, ключи, базу данных) ===
# Игнорируем содержимое data/, но сохраняем пустую директорию через .gitkeep
data/*
!data/.gitkeep

# Portable Node.js (тяжёлый, скачивается установщиком)
node/

# Зависимости npm
node_modules/

# Логи службы (NSSM)
logs/

# Системные файлы
*.log
.DS_Store
Thumbs.db

# VBScript-обёртка для скрытого запуска (создаётся установщиком)
start_hidden.vbs

# NSSM (скачивается установщиком для службы Windows)
install/windows/nssm/
